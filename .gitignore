# Local secrets — never commit real credentials/tokens
appsettings.Local.json
**/appsettings.Local.json

# Saved chat history (personal, per-machine)
conversation.json
**/conversation.json

# Saved answer cache (personal, per-machine)
answers.json
**/answers.json

# Build output
bin/
obj/

# User-specific / IDE
.vs/
*.user

# Generated agent binaries — built by Docker on deploy (and locally); never commit.
# The 97 MB kr7.exe is produced at build time, so it must NOT be pushed to git.
dist/
InterviewPrep/downloads/
