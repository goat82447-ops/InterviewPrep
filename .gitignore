# Local secrets — never commit real credentials/tokens
appsettings.Local.json
**/appsettings.Local.json

# Saved chat history (personal, per-machine)
conversation.json
**/conversation.json

# Build output
bin/
obj/

# User-specific / IDE
.vs/
*.user
